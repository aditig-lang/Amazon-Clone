# Amazon Clone (static)

Quick local dev server for the static site.

Run locally:

```bash
npm run dev
```

Then open http://localhost:3000 in your browser. To use a different port:

```bash
PORT=8080 npm run dev
```
